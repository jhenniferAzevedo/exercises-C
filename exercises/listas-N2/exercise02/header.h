/* Mostra as opções de operações pro usuário */
void menu(void);

/* Mostra o resultado da soma de dois números. */
void addition(double, double);

/* Mostra o resultado da subtração do primeiro número pelo segundo. */
void subtraction(double, double);

/* Mostra o resultado da multiplicação de dois números. */
void multiplication(double, double);

/* Mostra o resultado da divisão do primeiro número pelo segundo. */
void division(double, double);

/* Troca o valor escolhido pelo usuário. */
void change_number(double *, double *);